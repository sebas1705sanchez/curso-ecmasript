let hello = 'Hello';
let world = 'world';
let epicPhrase = hello + ' ' + world;
console.log(epicPhrase);

// Tamplate literals
let epicPhrase2 =  `${hello} ${world}`
console.log(epicPhrase2);

let lorem = `Esto es una frase epica
y esto es el cambio de linea`;
console.log(lorem);